{
  "name": "es5-webpack",
  "version": "1.0.0",
  "description": "ES5 scripts (i.e. no classes); bundled with webpack; no further transpilation (i.e. not using Babel).",
  "license": "MIT",
  "scripts": {
  	"clean": "rm -rf build/*",
  	"build": "npm run clean && webpack"
  },
  "devDependencies": {}
}

{
  "name": "ts",
  "description": "Compile TS to JS",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
  	"clean": "rm -rf build/*",
  	"ts": "../node_modules/.bin/tsc",
    "xxxbuild": "npm run clean && npm run ts",
  	"build": "echo \"Error: haven't finished setting up build process yet...\" && exit 1"
  },
  "devDependencies": {}
}
